# Connect any AI to Suma

Universal contract:

```txt
MCP server id: sumanos
Endpoint:      https://api.sumanos.com/mcp/authoring
Bearer env:    SUMANOS_KEY
```

Each host uses its own adapter. The adapter packages instructions, skills, prompts and MCP config; the protocol remains Sumanos MCP.
